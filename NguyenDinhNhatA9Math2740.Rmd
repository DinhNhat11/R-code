---
title: "NguyenDinhNhatA9Math2740"
author: "DinhNhatNguyen"
date: "2024-11-05"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Question 1

```{r}
library("igraph")

edges = c("a","b","b","c","c","d","d","e","e","f","f","g","g","h","h","i","i","a","a","j","d","j","g","j","c","e","b","f","e","i","f","h")
edge_matrix = matrix(edges, nc = 2, byrow = T)
edge_matrix

plot(graph_from_edgelist(edge_matrix,directed = F))

```
```{r}

library("igraph")

# Define the edges as a vector of vertex pairs
edges <- c("a", "b", "b", "c", "c", "d", "d", "e", "e", "f", "f", "g", "g", "h", "h", "i", "i", "a", 
           "a", "j", "d", "j", "g", "j", "c", "e", "b", "f", "e", "i", "f", "h")

# Convert the edge list to a matrix with 2 columns
edge_matrix <- matrix(edges, nc = 2, byrow = TRUE)

# Create an undirected graph from the edge list
g <- graph_from_edgelist(edge_matrix, directed = FALSE)

# Specify layout, placing vertex 'j' at the center
layout <- layout_with_fr(g)
layout[V(g)$name == "j", ] <- c(0, 0)  # Manually position 'j' at the center

# Plot the graph
plot(g, layout = layout, vertex.size = 20, vertex.label.color = "black",
     vertex.color = "skyblue", edge.color = "gray", main = "Graph with Central Node 'j'")


```






```{r}
library("igraph")

# Define the edges as specified in the problem
edges <- c("a", "b", "b", "c", "c", "d", "d", "e", "e", "f", "f", "g", "g", "h", "h", "i", "i", "a", 
           "a", "j", "d", "j", "g", "j", "c", "e", "b", "f", "e", "i", "f", "h")

# Convert the edge list to a matrix with 2 columns
edge_matrix <- matrix(edges, nc = 2, byrow = TRUE)

# Create an undirected graph from the edge list
g <- graph_from_edgelist(edge_matrix, directed = FALSE)

# Customize the vertex names and plot settings
plot(g, vertex.size = 20, vertex.label.color = "black", vertex.color = "skyblue",
     edge.color = "gray", main = "Enhanced Graph with Extra Connections")


```
```{r}

library("igraph")

# Define the edges as specified
edges <- c("a", "b", "b", "c", "c", "d", "d", "e", "e", "f", "f", "g", "g", "h", "h", "i", "i", "a", 
           "a", "j", "d", "j", "g", "j", "c", "e", "b", "f", "e", "i", "f", "h")

# Convert the edge list to a matrix with 2 columns
edge_matrix <- matrix(edges, nc = 2, byrow = TRUE)

# Create an undirected graph from the edge list
g <- graph_from_edgelist(edge_matrix, directed = FALSE)

# (a) Eccentricity of vertex 'a'
eccentricity_a <- eccentricity(g, vids = "a")
cat("Eccentricity of vertex 'a':", eccentricity_a, "\n")

# (b) Radius of G
radius_g <- radius(g)
cat("Radius of G:", radius_g, "\n")

# (c) Diameter of G
diameter_g <- diameter(g)
cat("Diameter of G:", diameter_g, "\n")

# (d) Center of G
center_g <- center(g)
cat("Center of G:", center_g, "\n")



```
```{r}
library("igraph")

# Define the edges as specified
edges <- c("a", "b", "b", "c", "c", "d", "d", "e", "e", "f", "f", "g", "g", "h", "h", "i", "i", "a", 
           "a", "j", "d", "j", "g", "j", "c", "e", "b", "f", "e", "i", "f", "h")

# Convert the edge list to a matrix with 2 columns
edge_matrix <- matrix(edges, nc = 2, byrow = TRUE)

# Create an undirected graph from the edge list
g <- graph_from_edgelist(edge_matrix, directed = FALSE)

# Calculate eccentricities of all vertices
eccentricities <- eccentricity(g)

# (a) Eccentricity of vertex 'a'
eccentricity_a <- eccentricities["a"]
cat("Eccentricity of vertex 'a':", eccentricity_a, "\n")

# (b) Radius of G (minimum eccentricity)
radius_g <- min(eccentricities)
cat("Radius of G:", radius_g, "\n")

# (c) Diameter of G (maximum eccentricity)
diameter_g <- max(eccentricities)
cat("Diameter of G:", diameter_g, "\n")

# (d) Center of G (vertices with eccentricity equal to the radius)
center_g <- names(eccentricities[eccentricities == radius_g])
cat("Center of G:", center_g, "\n")


geodesic_distances <- distances(g)

# Print geodesic distances matrix
cat("Geodesic Distances Matrix:\n")
print(geodesic_distances)

# Optionally, find the geodesic distance between specific vertices (e.g., 'a' and 'j')
distance_a_to_j <- distances(g, v = "a", to = "j")
cat("Geodesic distance from 'a' to 'j':", distance_a_to_j, "\n")
```

